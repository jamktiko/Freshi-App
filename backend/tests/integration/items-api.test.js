import request from 'supertest';
// import app from '../../app.js'; // The express app

describe('Items API - Full CRUD Flow', () => {
  
  test.todo('POST /items — creates a new product with Flour 2027-01-01');
  
  test.todo('GET /items — verifies the created product appears in the list');
  
  test.todo('PUT /items/:id — changes the expiration date to 2026-06-01');
  
  test.todo('DELETE /items/:id — removes the product');
  
  test.todo('GET /items — verifies the product no longer appears after deletion');

});
