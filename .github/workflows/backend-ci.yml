name: Backend CI Pipeline

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    name: Run Tests & Checks
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      
    - name: Setup Node.js 24
      uses: actions/setup-node@v4
      with:
        node-version: '24'
        # cache: 'npm' # Uncomment this when a package.json is added to the repo
        
    - name: Install Dependencies
      run: |
        echo "Installing dependencies..."
        # npm ci # Uncomment when package.json is added
        
    - name: Run Linter / Formatting Checks
      run: |
        echo "Running Linters..."
        # npm run lint # Placeholder for ESLint/Prettier
        
    - name: Run Unit Tests
      run: |
        echo "Running Unit Tests..."
        # npm test # Placeholder for Jest/Mocha
